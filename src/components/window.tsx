"use client";

import { useEffect, useState } from 'react';
import { Button } from 'react95';

const Window = () => {
  return (
    <div className='w-full h-full bg-windows border-4 p-10'>
      <Button> Default</Button>
    </div>
  );
};

export default Window;